Python bindings for the linesweeper Rust crate, performing boolean operations on paths (intersection, union, difference, xor).

## Install

To install or update to the latest released package, run:

```
pip3 install --upgrade linesweeper
```

## Build

This package is built using the maturin build system, which compiles the Rust code and creates a Python wheel. To build the package locally, run:

```
maturin build --release
```

## License

This project is licensed under the Apache 2.0 License. See the [LICENSE.txt](LICENSE.txt) file for details.